# DocMorph Slim 运行依赖（GitHub 主仓库 / Source 版）
#
# 仅包含运行所需的直接第三方包；传递依赖由 pip 自动解析。
# 不包含：测试工具、静态检查、打包工具（如 pytest / ruff / pyinstaller）。
#
# 不含外部软件。「→PDF」高保真输出另需 Microsoft Word/Excel、WPS Office
# 或 LibreOffice 之一；运行 `python -m docmorph doctor` 查看本机检测结果。
#
# 版本范围与 pyproject.toml dependencies 保持一致。

PyMuPDF>=1.24
pdf2docx>=0.5.8
python-docx>=1.1
python-pptx>=0.6.23
pandas>=2.0
openpyxl>=3.1
Pillow>=10.0
pypandoc-binary>=1.13
pywebview>=5.0
pywin32>=306; sys_platform == "win32"
